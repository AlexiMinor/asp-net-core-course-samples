let a = '1';

if (a > 18) {
    //todo add classes and div-s with successful validation 
    if (COND) {

    }
    else if (COND) {
    } else {
    }
}
    else {
    alert("Invalid age");
    //todo add classes and div-s with validation error 
}

a > 15
    ? console.log('a>15')
    : console.log('a<=15')

switch (a) {
    case '1': // a === '1'
    {
        break;
    }
    case 1: // a === '1'
    {
        break;
    }
    default:
        break;
}